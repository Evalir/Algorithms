#include <bits/stdc++.h>
using namespace std;

int main() {
	int n;
	cin >> n;
	vector<int> s(n);
	for(auto &t : s) cin >> t;
	vector<int> ans;
	bool done = false;
	while(!done) {
		done = true;
		for(int i = 0; i < n; i++) {
			if (s[i] != i+1) {
				ans.push_back(
			}
		}
	}
	
}
