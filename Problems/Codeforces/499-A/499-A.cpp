#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>
using namespace std;

int main() {
	int n, x;
	cin >> n >> x;
	int ans = 0;
	int curr = 1;
	for(int i = - 0; i < n; i++) {
		int l, r;
		cin >> l >> r;
		while
	}
}