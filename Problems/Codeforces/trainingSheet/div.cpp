#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>
using namespace std;
typedef long long ll;

int main() {
	ll a, b, c;
	cin >> a >> b >> c;

	if (x > a && x > b) {
		cout << 0 << endl;
		return 0;
	}
	else {
		ll mi = min(a,b);
		ll ft;
		mi % c == 0 ? ft = mi : ft = ceil(((1.00 * mi)/c)) * 1LL;
		ll an = 



	}
}