#include <bits/stdc++.h>
using namespace std;
using Long = long long;


int main() {
    int n; Long t;
    scanf("%d%lld", &n, &t);
    vector<Long> V(n);
    for(auto &t : V) scanf("%lld", &t);

    return 0;
}